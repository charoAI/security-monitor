version: '3.8'

services:
  security-monitor:
    build: .
    ports:
      - "80:5000"
    volumes:
      - ./data:/app/data
      - ./article_cache:/app/article_cache
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped